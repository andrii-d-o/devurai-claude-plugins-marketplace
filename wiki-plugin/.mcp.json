{
  "mcpServers": {
    "wiki": {
      "command": "npx",
      "args": ["tsx", "${CLAUDE_PLUGIN_ROOT}/src/index.ts"],
      "env": {
        "WIKI_PATH": "${CLAUDE_PROJECT_DIR}/wiki"
      }
    }
  }
}
